[package]
name = "tunic-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
evm-engine = { path = "../evm-engine" }
clap = { version = "4.4", features = ["derive"] }
rayon = "1.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[[bin]]
name = "tunic"
path = "src/main.rs"
